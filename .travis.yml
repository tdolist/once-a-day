language: rust
os:
  - osx
  - linux

rust:
  - stable
  - beta
  - nightly
matrix:
  linux:
      -before_install:
        - sudo apt-get -qq update
        - sudo apt-get install -y gcc pkg-config libdbus-1-dev openssl libssl-dev
  allow_failures:
    - rust: nightly
